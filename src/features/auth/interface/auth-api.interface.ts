export interface AuthResponse {
    id: number;
    email: string;
    username: string;
    token: string;
}

export interface IUserResponse {
    id: string;
    email: string;
    username: string;
}